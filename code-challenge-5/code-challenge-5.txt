Write a JavaScript function that calculates the factorial of a given positive integer. 
The factorial of a number n is the product of all positive integers less than or equal to n. 
The factorial of 0 is 1.

For example, the factorial of 5 is calculated as 5 x 4 x 3 x 2 x 1 = 120.

Write the JavaScript function to calculate the factorial.